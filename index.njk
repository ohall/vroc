{% for chart in charts %}
    <a-image src="#{{chart.src}}" width="{{common.width}}" height="{{common.height}}" material="" geometry="" visible="" position="{{chart.x}} {{chart.y}} {{chart.z}}" rotation="0 {{chart.rotation}} 0"></a-image>
{% endfor %}
